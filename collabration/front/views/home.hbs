{{>header}}
{{>navbar}}
  <section class="row">
    <table class="table table-stripped">
      <thead>
        <tr>
          <th>Acc no.</th>
          <th>name</th>
          <th>Balance</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        {{#each allCustomers}}
        <tr>
          <td>{{id}}</td>
          <td>{{name}}</td>
          <td>{{balance}}</td>
          <td>
             <a href="/single/{{id}}" class="btn btn-primary mx-2">Show</a>
             {{!-- <a href="/edit/{{id}}" class="btn btn-warning mx-2">Edit</a> --}}
            <a href="/delete/{{id}}" class="btn btn-danger mx-2">Delete</a> 
          </td>
        </tr>
  {{/each}}
      </tbody>
    </table>
  </section>
</div>
{{>footer}}